/*
class MobileClient: ClientBase(this::class.java.simpleName, true, true, true) {
	override fun getSession(setupSession: (clientConfig: HttpClientConfig) -> Unit, args: List<Any>, kwargs: Dictionary<String, Any>): Base = Mobileclient(setupSession, args, kwargs)
	
	val androidId: String = "";
	
	fun ensureDeviceId(deviceId: String) {
		if (deviceId.isEmpty())
			deviceId = androidId
		
		if (deviceId.length == 16 && )
	}
	
	
}*/
